apiVersion: v2
name: kraken-auth
description: Kraken Auth service
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: postgresql
    version: 15.2.0        # example current series; pick a recent one
    repository: oci://registry-1.docker.io/bitnamicharts
    condition: postgresql.enabled